function sub_data = subsample_fun(data,num_observations_needed)

for i = 1:num_iterations
    sub_data = randsample(data,num_observations_needed);
%     velocity_bin_trials_subsampled{m,b,i} = sub_data;
end
